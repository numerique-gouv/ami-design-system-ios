name: Ami-Design-System-Lab
#--------------------------------------------------------------------------------
schemeTemplates:
  BaseScheme:
    analyze:
      config: Debug
    archive:
      config: Release
    build:
      targets:
        ${scheme_name}:
        - running
        - testing
        - profiling
        - analyzing
        - archiving
    profile:
      config: Release
    run:
      config: Debug
    test:
      config: Debug
      gatherCoverageData: true
      coverageTargets:
        - ${scheme_name}:
      testPlans:
#         - path: UnitTests/SupportingFiles/UnitTests.xctestplan
#           defaultPlan: true
#         - path: UITests/SupportingFiles/UITests.xctestplan
#           defaultPlan: false
#         - path: PreviewTests/SupportingFiles/PreviewTests.xctestplan
#           defaultPlan: false
#--------------------------------------------------------------------------------
schemes:
  Ami-Design-System-Lab-Production:
    templates:
      - BaseScheme
#--------------------------------------------------------------------------------
targetTemplates:
  BaseTarget:
    type: application
    platform: iOS

    info:
      properties:
        UILaunchScreen:
          UIColorName: colors/background-color
        CFBundleDisplayName: $(APP_DISPLAY_NAME)
        CFBundleShortVersionString: $(MARKETING_VERSION)
        CFBundleVersion: $(CURRENT_PROJECT_VERSION)
        UISupportedInterfaceOrientations: [
          UIInterfaceOrientationPortrait,
          UIInterfaceOrientationPortraitUpsideDown,
          UIInterfaceOrientationLandscapeLeft,
          UIInterfaceOrientationLandscapeRight
        ]
        ITSAppUsesNonExemptEncryption: false
#        NSCameraUsageDescription: To take pictures or videos and send them as a message $(APP_DISPLAY_NAME) needs access to the camera.
#        NSMicrophoneUsageDescription: To record and send messages with audio, $(APP_DISPLAY_NAME) needs to access the microphone.
#        NSPhotoLibraryAddUsageDescription: This lets you save images and videos to your photo library.
#        NSLocationWhenInUseUsageDescription: Grant location access so that $(APP_DISPLAY_NAME) can share your location.
#        NSFaceIDUsageDescription: Face ID is used to access your app.
        UIBackgroundModes: [
          remote-notification
        ]
        UIApplicationSceneManifest:
          UIApplicationSupportsMultipleScenes: false
          UISceneConfigurations: []
        LSRequiresIPhoneOS: true
        UILaunchScreen:
          UILaunchScreen: {}
# Disable Firebase swizzling to have AppDelegate callback in SwiftUI app: https://firebase.google.com/docs/cloud-messaging/ios/get-started#disable-swizzling
        FirebaseAppDelegateProxyEnabled: false
        
    settings:
      PRODUCT_NAME: $(APP_NAME)
      PRODUCT_BUNDLE_IDENTIFIER: $(BASE_BUNDLE_IDENTIFIER)
      ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
      DEVELOPMENT_ASSET_PATHS: ami-design-system-lab/Resources/Preview
      ASSETCATALOG_COMPILER_GENERATE_SWIFT_ASSET_SYMBOL_EXTENSIONS: false # Done by SwiftGen
      ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: text/title/blue-france
#       SWIFT_OBJC_BRIDGING_HEADER: Project-Bridging-Header.h
#       SWIFT_OBJC_INTERFACE_HEADER_NAME: GeneratedInterface-Swift.h

    preBuildScripts:
    - name: 🛠 SwiftGen
      runOnlyWhenInstalling: false
      basedOnDependencyAnalysis: false
      shell: /bin/sh
      script: |
        scripts/ami-xcode-pass-swiftgen.sh
        
    postBuildScripts:
    - name: ⚠️ SwiftLint
      runOnlyWhenInstalling: false
      basedOnDependencyAnalysis: false
      shell: /bin/sh
      script: |
        scripts/ami-xcode-pass-swiftlint.sh
        
    - name: 🧹 SwiftFormat
      runOnlyWhenInstalling: false
      basedOnDependencyAnalysis: false
      shell: /bin/sh
      script: |
        scripts/ami-xcode-pass-swiftformat.sh

    dependencies:
    - package: SwiftUI-Introspect
      product: SwiftUIIntrospect
      embed: false
    - package: AmiDesignSystem
      product: AmiDesignSystem
      
    sources:
    - path: ami-design-system-lab/Sources

#--------------------------------------------------------------------------------
targets:
#--------------------------------------------------------------------------------
  Ami-Design-System-Lab-Production:
    templates: 
      - BaseTarget

    info:
      path: ami-design-system-lab/SupportingFiles/Info.plist

    settings:
      BASE_BUNDLE_IDENTIFIER: fr.gouv.ami-design-system-lab
      PRODUCTION_APP_NAME: AMI DESIGN SYSTEM LAB
      APP_NAME: Ami-Design-System-Lab-Production
      APP_DISPLAY_NAME: AMI DS Lab # The name used in the application.
      CODE_SIGN_ENTITLEMENTS: ami-design-system-lab/SupportingFiles/ami-design-system-lab.entitlements
      OTHER_SWIFT_FLAGS: $(inherited) -D IS_MAIN_APP -D IS_AMI_DESIGN_SYSTEM_LAB_PRODUCTION
#--------------------------------------------------------------------------------
